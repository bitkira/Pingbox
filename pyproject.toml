[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "openchat"
version = "0.1.0"
description = "Managed local multi-agent messaging runtime for Codex."
requires-python = ">=3.11"

[project.scripts]
openchat = "openchat.cli:main"
