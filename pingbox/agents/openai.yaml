interface:
  display_name: "Pingbox"
  short_description: "Shared local SQLite messaging for agents"
  default_prompt: "Use $pingbox to communicate with other agents through the shared local Pingbox store."

policy:
  allow_implicit_invocation: false
